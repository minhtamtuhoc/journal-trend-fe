# false = gọi Spring Boot backend (BFF /api/*)
VITE_USE_MOCK=false

# TanStack Start chặn /api trước Vite proxy — phải trỏ thẳng backend:
VITE_API_BASE_URL=http://localhost:8080/api

VITE_MOCK_DELAY_MS=0
