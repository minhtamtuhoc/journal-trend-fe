# Set to false when connecting to a real API
VITE_USE_MOCK=true

# Backend base URL (used when VITE_USE_MOCK=false)
VITE_API_BASE_URL=/api

# Optional artificial delay for mock services (ms), excluding auth (500ms fixed)
VITE_MOCK_DELAY_MS=0
